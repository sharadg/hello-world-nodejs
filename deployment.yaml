---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  namespace: default
  name: nodejs-app
  labels:
    app: nodejs-app
spec:
  replicas: 1
  template:
    metadata:
      namespace: default
      labels:
        app: nodejs-app
    spec:
      securityContext:
        runAsUser: 1000
        runAsGroup: 3000
        fsGroup: 2000
      containers:
        - name: nodejs-app
          image: harbor.home.pcfdot.com/library/hello-world-nodejs@sha256:$DIGEST_VERSION
          imagePullPolicy: Always
          securityContext:
            allowPrivilegeEscalation: false
# ---
# apiVersion: v1
# kind: Service
# metadata:
#   namespace: default
#   name: nodejs-app-svc
# spec:
#   ports:
#     - name: http
#       port: 80
#       protocol: TCP
#       targetPort: 8080
#   selector:
#     app: nodejs-app
#   type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  namespace: default
  name: nodejs-app-np
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 8080
  selector:
    app: nodejs-app
  type: NodePort
